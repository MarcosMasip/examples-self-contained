{
  "name": "serve-static",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "test": "echo 'No tests for serve-static. Run: npm -w serve-static run dev  # then open http://localhost:8787/' && exit 0"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "^4.2.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "vitest": "^1.5.0",
    "wrangler": "^3.53.0"
  }
}
