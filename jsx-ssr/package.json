{
  "name": "jsx-ssr",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev --live-reload src/index.tsx",
    "deploy": "wrangler deploy src/index.tsx",
    "test": "echo 'No tests for jsx-ssr. Run: npm -w jsx-ssr run dev  # then open http://localhost:8787/' && exit 0"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "^4.2.4"
  },
  "devDependencies": {
    "wrangler": "^3.53.0"
  }
}
