{
  "name": "hono-vite-jsx",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build",
    "test": "echo 'No tests for hono-vite-jsx. Run: npm -w hono-vite-jsx run dev  # then open http://localhost:5173/' && exit 0"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.2",
    "hono": "^4.5.10"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.1.0",
    "@hono/vite-dev-server": "^0.17.0",
    "@types/node": "^20.11.17",
    "vite": "^5.4.2"
  }
}